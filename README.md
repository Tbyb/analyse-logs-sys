# 📑 Projet 06 – Analyseur de logs (Linux & Windows)

## 🎯 Objectif
Extraire des erreurs critiques depuis :
- les journaux système Linux (`journalctl`, `grep`)
- les journaux Windows (`Get-EventLog`)